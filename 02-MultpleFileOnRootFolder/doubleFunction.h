#ifndef doubleFunction_H
#define doubleFunction_H

#include <stdio.h>

/**
 * @brief A simple function used only as an example
 *        prints to stdout "Executando doubleFunction!"
 *        and return the param multiplied by 2.
 * @param number
 * @return number * 2
 */
double doubleFunction(double number);

#endif